# Step scan script

## Simple script for running step scans based in Diamond Light Source.

### Instalation
Run:
    - source startup

This will install the module and all it's dependencies as well as source the virtual environment created.

### Running:
    - step_scan -f config/your_config_file.json